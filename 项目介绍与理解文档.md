# 一、课题概述

**毕设题目**：《中英双语语音对话大模型关键技术研究》。

**课题背景**：当前大模型、语音识别与语音合成技术的结合，使“语音输入—语言理解—语音输出”的智能对话系统成为可能。在本项目上下文中，目标并非通用问答产品，而是面向英语学习、口语陪练与教学辅助场景，构建一个可进行中英双语交互的语音对话系统。该系统既要支持中文指令与解释，又要支持英文练习、纠错、翻译和情景对话，因此其核心不只是功能堆叠，而是围绕双语语音对话链路中的关键技术问题展开研究。

**研究意义**：一方面，该课题具有明确的应用价值，可服务于中国学习者的英语口语训练、场景模拟与即时反馈；另一方面，该课题也具有较强的研究意义，涉及中英混合语音识别、垂直场景大模型微调、语音对话链路设计、实时交互稳定性与推理效率等多个典型问题，适合作为“语音对话大模型”方向的毕业设计研究对象。

**课题目标**：结合当前会话信息，课题目标可理解为：构建一个面向英语学习场景的中英双语语音对话系统，形成从 ASR、LLM 到 TTS 的完整技术链路，并围绕大模型在教学场景中的能力优化、全双工/实时交互、双语混合输入输出和评测对比等问题进行研究与实现。

# 二、项目应用场景与需求理解

**目标用户**：以中文母语或中文主导的英语学习者为主，尤其是需要进行口语陪练、场景模拟、语法纠错、双语解释和语音交互练习的用户。

**典型使用场景**：当前会话中已反复出现餐厅点餐、机场值机、购物、旅行英语、天气介绍、英语常识问答等场景；同时系统也支持普通文本聊天、语音识别输入、TTS 播报以及“电话模式/全双工模式”下的实时语音对话。

**用户核心需求**：用户希望系统既能“陪练”，又能“教学”。这包括：理解中文任务说明、生成英文对话、对用户英文表达进行纠错、解释词句用法、在必要时进行翻译，并在语音模式下实现尽量自然的打断、插话与实时反馈。

**为什么需要中英双语语音对话能力**：英语学习场景天然是双语混合的。用户常常用中文布置任务、询问语法、要求解释，同时又用英文完成练习或进行角色扮演；在语音模式下，这种中英切换更加频繁。因此系统必须具备中英双语理解与表达能力，而不是单纯的英文对话能力。

**为什么需要大模型而不是传统规则系统**：从当前会话可见，用户需求并不固定，既包含翻译、纠错、解释、角色扮演，也包含对上下文、教学意图和场景推进的要求。传统规则系统难以处理自由表达、上下文记忆与多任务切换，而大模型更适合承担语义理解、对话生成、教学反馈和场景泛化等复杂任务。

# 三、系统总体方案理解

**系统整体目标**：构建一个面向英语学习的中英双语语音对话系统，在保留成熟文本聊天能力的基础上，扩展到实时语音对话与全双工交互。

**系统总体架构**：从会话信息看，系统采用级联式语音链路，即 ASR + LLM + TTS。前端负责交互界面与部分语音侧控制，后端负责会话管理、模型调度、语音识别、文本生成和语音合成。系统同时保留 HTTP 文本接口与 WebSocket 全双工接口。

**ASR（语音识别）**：当前后端目标模型已从 Whisper 转向 SenseVoice，并通过 FunASR 进行推理。该模块服务于文本聊天中的语音输入，也服务于全双工语音链路。结合项目目标，其研究重点不仅是普通中文或英文识别，更包括中英混合输入、口语化表达以及 ASR 误识别后的上下文修正。

**LLM（对话大模型）**：系统早期曾使用或考虑过 Qwen2.5-1.5B-Instruct，后续已切换到 Qwen3-4B 作为基座模型，并支持可选 LoRA 微调版本。当前会话中明确出现了多个 LoRA 版本路径，如 `stage1-lora`、`stage1-v2-6000-lora`、`stage1-v3-closing-lora`。同时，后端对话策略已从“多模式强约束”调整为“统一 prompt + 少量辅助判断”的轻策略方案，以减少对微调模型原生能力的干扰。

**TTS（语音合成）**：TTS 路线经历了多次尝试，包括 gTTS、Edge TTS、CosyVoice、MeloTTS，最终当前可见的主要实现路线为 MOSS-TTS-Nano，并尝试使用 CUDA 加速。TTS 既服务于文本聊天中的播报，也服务于全双工语音输出。当前上下文表明，中英混读、音色一致性、合成速度与前端播放流畅性仍是重要问题。

**前后端/服务接口**：后端保留了原有 Flask 文本接口，同时新增 FastAPI/ASGI WebSocket 能力。全双工接口路径为 `/ws/full-duplex`，文本相关接口包括 `/api/chat`、语音识别上传与系统 prompt 导出等。前端后续还加入了训练数据导出需求，后端配合提供了真实 system prompt 接口，以支持 SFT 数据构造。

**各模块之间的数据流与调用关系**：在文本聊天模式下，用户文本或上传语音先进入后端，语音经 ASR 转写后送入 LLM，LLM 生成回复文本，再按需调用 TTS 生成音频并返回。全双工模式下，前端持续采集语音并通过协议推送给后端，后端进行 ASR、上下文管理和 LLM 推理，再将文本与合成音频返回前端；当检测到用户插话时，系统尝试中断当前播报并转入新的语音轮次。当前方案中，VAD 责任已从后端方案迁移到前端接口侧，后端更侧重会话编排与模型调度。

# 四、关键技术研究点分析

## 1. 中英双语与中英混合语音识别问题

**问题是什么**：用户并非严格使用单一语言，而是常常在中文任务说明、英文练习内容、混合语句之间切换。这会使 ASR 面临普通识别之外的中英混合识别与分段问题。

**为什么重要**：如果 ASR 不能稳定识别双语混合输入，后续的纠错、翻译、角色扮演与语音输出都会失真，直接影响教学效果。

**上下文体现在哪里**：当前会话中，用户多次用中文布置英语练习任务，并在全双工模式下口述英文句子、询问中英文表达。系统也已从 Whisper 迁移到 SenseVoice，说明识别模型选择本身就是系统优化重点。

**可能的解决思路**：继续围绕 SenseVoice 的双语场景适配与后处理优化展开；在系统层面，结合上下文对 ASR 结果进行语义修正；在数据层面，后续可增加中英混合口语样本用于评估或进一步训练。部分流式/准流式中间结果设计在会话中被明确提出，但其最终实现细节仍有待持续验证。

## 2. 英语教学/口语陪练场景下的大模型能力约束与优化

**问题是什么**：通用大模型虽然能对话，但在英语教学场景中容易出现复读、乱切模式、过度反问、翻译与纠错混淆、上下文记忆不足等问题。

**为什么重要**：该课题的目标不是通用聊天，而是“教学型对话”。如果模型无法稳定扮演英语教练、做出合理纠错与解释，就难以体现课题的研究价值。

**上下文体现在哪里**：整个会话中，用户多次反馈模型会重复上一句、把翻译做错、在角色扮演中乱跳、把用户的纠错请求误判为其他任务等。后端 prompt 与 dialogue policy 被多轮修改，最终又被刻意简化，正说明“如何约束而不过度约束模型”本身就是研究问题。

**可能的解决思路**：继续采用“轻策略辅助 + 模型能力主导”的思路，后端只保留少量必要辅助，把更多精力放到微调数据、任务设计与评测机制上，从数据侧而非模板侧提升模型的教学表现。

## 3. 面向垂直场景的指令微调与 LoRA 微调

**问题是什么**：基座模型在英语学习场景中往往无法直接达到理想效果，尤其在纠错、翻译、角色扮演推进、任务收束等方面存在明显不足。

**为什么重要**：课题题目直接指向“大模型关键技术研究”，其中面向垂直场景的参数高效微调是核心技术路线之一。

**上下文体现在哪里**：当前会话明确出现了多个 LoRA 权重版本，并支持后端启动时切换 `base` 或 `lora`。用户也多次对微调前后模型效果、重复问题、训练权重更新以及评测对比提出要求。

**可能的解决思路**：围绕英语口语陪练、纠错、翻译、场景推进、收束语气等问题继续构造 SFT 数据，采用 LoRA 进行多轮增量微调；对每一轮新权重建立标准化对比评测，逐步缩小“工程修补”与“模型本体能力提升”之间的差距。

## 4. 训练数据构造与 SFT 数据设计

**问题是什么**：如果训练数据分布单一，例如大量反问句、固定格式纠错或模板化教师话术，模型就容易学成“只会问问题”“只会按格式回答”的模式。

**为什么重要**：在教学场景中，训练数据的风格直接决定模型输出风格；数据问题往往比 prompt 问题更深层。

**上下文体现在哪里**：用户明确指出某轮微调后模型“会一直问”，并推测是因为训练数据里反问句太多；前端又新增了聊天记录导出功能，要求导出真实 system prompt 与 role/content 对话，以便用于训练。

**可能的解决思路**：一方面继续完善前端导出训练数据链路，保证 system prompt 与对话历史真实一致；另一方面从数据层面增加非反问式回复、任务完成后的收束回复、自然纠错与忠实翻译样本，减少模型被单一语气模板“带偏”的风险。

## 5. 中英混读语音合成与语音自然度问题

**问题是什么**：中英混读时，TTS 容易出现音色跳变、分句混乱、漏合成、停顿不自然等问题。

**为什么重要**：本课题并非纯文本系统，而是“中英双语语音对话”系统。若 TTS 在双语输出中不稳定，整体交互体验会明显下降。

**上下文体现在哪里**：会话中多次出现中英混合句子合成异常、音色不一致、分段有误、整句漏播、前端播放卡顿等问题。TTS 路线也经历了多次技术选型调整，说明该问题尚未完全解决。

**可能的解决思路**：在模型层面继续比较不同 TTS 路线对中英混读的支持能力；在系统层面优化句子分段、整句发送与播放队列；在后续研究中，可将“中英混读 TTS 的自然度与一致性”单独作为课题子问题进一步展开。

## 6. 全双工或实时语音交互中的延迟与稳定性问题

**问题是什么**：全双工语音交互不仅要求“能听能说”，还要求能被打断、能快速停播、能在多轮中保持上下文一致，并尽量减小延迟与卡顿。

**为什么重要**：实时性与可打断性决定了系统是否接近自然对话体验，也是语音对话系统区别于普通文本问答的重要技术门槛。

**上下文体现在哪里**：用户最初明确提出“只要服务端在播 TTS 时检测到用户真实说话就立刻停播”，并强调宁可偶尔误打断，也要敏感抓住插话。后续全双工链路、VAD 前移、整句音频发送与播放队列等，都是围绕这一问题展开。

**可能的解决思路**：继续完善前后端协议与中断机制，优化前端 VAD 与后端会话调度配合；在后端尽量缩短 LLM 与 TTS 响应延迟；在历史管理上坚持“仅将实际播出的 assistant 内容纳入上下文”的原则，以降低被中断后上下文错位的风险。相关实现细节是否已完全闭环，当前信息仍不完整。

## 7. 对话质量评测与教学效果评测问题

**问题是什么**：如果没有系统评测，就很难判断某轮微调、某次 prompt 调整、某次后端策略简化到底是否真的改进了模型能力。

**为什么重要**：毕业设计中的“关键技术研究”不仅要实现系统，还需要提出有依据的效果分析与模型比较。

**上下文体现在哪里**：用户已明确提出希望对微调前后模型进行对比，并讨论了盲测对比、分类维度、基础模型与 LoRA 模型的分层评测思路。

**可能的解决思路**：建立以 Base 与 LoRA 为对比对象的任务集，覆盖翻译、纠错、角色扮演、上下文保持、收束能力、重复率等维度；优先采用盲测对比与胜负统计，而非仅靠主观打分。当前尚未出现正式评测结果，因此实验数据与结论均待确认。

# 五、模型与训练方案理解

**当前使用或考虑的模型**：ASR 方面，当前目标模型为 SenseVoice，早期路径中存在 Whisper。LLM 方面，项目后期主模型已切换到 Qwen3-4B，并支持基座模型与 LoRA 微调模型切换；早期也出现过 Qwen2.5-1.5B-Instruct 路线。TTS 方面，当前稳定路线主要是 MOSS-TTS-Nano，早期则尝试过 gTTS、Edge TTS、CosyVoice、MeloTTS 等方案。

**选择这些模型的原因**：从当前会话看，模型选择主要围绕面向中英双语与英语教学场景的适配性，以及推理速度与工程可实现性展开。换言之，模型并非只看学术性能，而是兼顾任务表现与系统实现需求。

**微调目标**：微调目标主要集中在英语教学与口语陪练能力上，包括但不限于：更自然的场景对话推进、更稳定的翻译、更实用的纠错、更少的复读、更好的上下文保持，以及更符合教学语境的收束与追问方式。

**训练数据来源与构造思路**：当前会话已明确存在通过前端导出对话记录来构造训练数据的思路，并且导出内容要求包含真实 system prompt 与按角色组织的对话历史，以便用于 SFT。除此之外，是否还有人工整理样本、自动生成样本或其他数据来源，当前信息不完整，需标注为待确认。

**训练方式理解**：当前已明确采用 LoRA 方式进行多轮微调，并且存在多个阶段性权重版本。是否同时系统使用了 QLoRA、具体超参数设置、训练轮数与样本规模，当前会话未给出完整信息，故应标注为待确认。

**可能的评测维度**：从已讨论内容看，可用的评测维度包括：翻译正确性、纠错有效性、上下文保持、重复率、语言切换稳定性、任务收束能力、场景推进合理性、延迟与推理速度等。正式评测方案已被讨论，但尚无明确实验结果。

**已讨论过的优化方向**：当前已讨论并部分实施的优化方向包括：将后端从多模式强约束改为统一 prompt 轻约束；增强翻译目标抽取；放宽纠错输出格式；支持基座模型与 LoRA 的切换比较；通过导出真实对话记录支持持续微调。

# 六、当前项目进展与已有基础

**当前已经具备的内容**：项目已具备文本聊天能力，并明确要求保留该成熟方案；语音识别、TTS 播报和全双工语音链路均已接入；后端文本接口与全双工接口可以共存；SenseVoice、Qwen3-4B、MOSS-TTS-Nano 等核心模型均已完成过加载与运行。

**当前正在推进的内容**：当前重点仍在模型效果优化与策略调整上，尤其是翻译、纠错、复读、上下文保持、中英混读 TTS、微调权重迭代以及基座/LoRA 效果对比。

**从上下文中可以看出的系统基础**：系统已经不仅仅是概念验证，而是具备了较完整的工程雏形，包括：文本与语音双模式、LoRA 切换机制、全双工接口、训练数据导出链路，以及较为完整的后端服务组织方式。

**当前遇到的具体问题或报错**：会话中出现过多类问题，包括 CosyVoice 加载失败、gTTS 网络不可达、MOSS-TTS-Nano 中英混读表现不稳、模型复读、翻译模式误判、纠错格式过硬、前端/手机网络访问受校园网限制等。这些问题说明项目已进入“复杂系统联调与质量打磨”阶段。

**现阶段的主要瓶颈**：现阶段最主要的瓶颈不是“能不能跑起来”，而是“能否稳定地表现得像一个好用的中英双语英语教练”。具体表现为：大模型教学表现不稳定、双语翻译与纠错容易混淆、语音链路的实时性与自然度仍待提升。

# 七、存在问题与挑战

**研究层面的挑战**：一是如何让大模型在英语教学场景中既自然又稳定，而不是依赖大量手工规则；二是如何让模型同时兼顾翻译、纠错、解释、角色扮演和上下文保持；三是如何处理双语混合输入输出，尤其是中英混说、中英混读；四是如何建立有说服力的评测体系来比较基座模型与微调模型。

**工程实现层面的挑战**：主要包括多模型协同复杂、依赖冲突、推理链路长导致时延大、全双工交互调度复杂、移动端网络接入受限等。

**哪些问题最影响最终效果**：从当前会话看，最影响最终效果的是 LLM 的教学质量与稳定性，包括复读、误判、翻译不忠实、纠错不自然、上下文弱等问题；其次是中英混读 TTS 的自然度问题。

**哪些问题最影响落地效果**：最影响整体落地效果的是模型稳定性、语音链路时延、中英混读 TTS 表现，以及前后端交互过程中的一致性与鲁棒性。

# 八、后续研究与实现方向

**短期可推进任务**：继续验证简化后的统一 prompt 策略；修正翻译与纠错边界问题；对最新 LoRA 权重进行系统回归测试；继续优化全双工链路中的打断与播放体验；对中英混读 TTS 做更细粒度排查。

**中期优化方向**：围绕英语教学场景继续构建和清洗 SFT 数据，重点补足非反问式回复、任务完成后的收束回复、自然纠错与忠实翻译等样本；同时建立基座模型与 LoRA 模型的标准化对比评测流程。

**长期研究空间**：可进一步研究真正面向中英双语语音交互的垂直大模型方案，包括更适合英语教学的对话优化、更加自然的双语语音合成、更低时延的实时语音交互，以及更系统的教学效果评估。

**可进一步形成论文或毕设亮点的内容**：一是面向英语学习场景的中英双语语音对话系统设计；二是基于 Qwen3-4B 的参数高效微调在教学场景中的应用；三是全双工语音交互中的打断与上下文一致性问题；四是中英混合语音识别与语音合成的工程与算法问题。

# 九、总结

当前项目已经从单一功能验证逐步演进为一个具备完整技术链路的中英双语语音对话研究系统。系统已形成 ASR、LLM、TTS、文本聊天、全双工语音交互、LoRA 微调和训练数据导出等多项基础能力，说明其工程基础已经较为扎实。但从研究视角看，项目仍处于“系统已搭起、效果待打磨、评测待规范”的阶段。

该课题的核心价值在于：它并非单纯做一个语音聊天应用，而是围绕“中英双语语音对话大模型”的落地过程，持续暴露并研究关键技术问题，包括双语语音识别、垂直场景大模型微调、语音对话链路编排、中英混读语音合成与实时性等。结合当前上下文，下一步最值得优先突破的关键技术点，仍然是“教学场景下的大模型能力优化与标准化评测”，因为这将直接决定系统能否从“可运行”走向“可用、可信、可研究”。
