# 后端回复策略说明（新版）

本文档说明当前后端 `code/dialogue_policy.py` 的真实工作方式。

这版策略已经做了明显简化：不再把用户输入硬切成很多模式，也不再用大量模板强行约束模型，而是尽量让微调后的模型自己发挥。

## 一、当前只有 3 个聚焦类别

后端现在只保留 3 个轻量级 `focus`：

| focus | 说明 | 何时触发 |
| --- | --- | --- |
| `translation` | 显式翻译请求 | 用户明确说“翻译”“译一下”“translate” |
| `correction` | 显式纠错请求 | 用户明确说“语法错误吗”“纠正”“改一下”“correct”“fix”“natural”等 |
| `general` | 其他所有普通情况 | 除翻译、纠错外的全部输入 |

## 二、核心思路

旧版做法是：

- 先把输入细分成很多模式
- 每种模式单独写 prompt
- 再用很多规则拦截输出

新版做法是：

- 只判断用户是不是在“明确翻译”或“明确纠错”
- 其余情况全部交给统一 prompt
- 保留极少量必要的兜底规则

一句话概括：

后端现在主要做“辅助理解”，不再做“强行指挥”。

## 三、统一 Prompt 机制

现在绝大多数回复都走统一入口：

- `build_chat_prompt()`
- `_build_unified_prompt()`

统一 prompt 会告诉模型：

- 尽量自然理解用户真实意图
- 保留当前上下文，但不要死守角色扮演
- 如果用户明确要求翻译，就翻译被引用的句子
- 如果用户明确要求纠错，就做自然纠错
- 不要机械复读上一条 assistant 回复
- 只有在真的需要时才问下一个问题

也就是说，现在不是“后端给模型分配详细岗位”，而是“后端只给模型一个轻量方向”。

## 四、后端现在还保留哪些辅助能力

虽然整体简化了，但后端仍然保留了几项确实有用的辅助：

### 1. 翻译引用解析

当用户说：

- “翻译这句话”
- “翻译一下”
- “translate this sentence”

后端会尽量从最近上下文里找到真正要翻译的句子，而不是把“翻译这句话”本身拿去翻译。

### 2. 纠错目标定位

当用户说：

- “我刚刚说的话有语法错误吗”
- “帮我改一下”
- “correct this”

后端会优先把最近一条像英语练习内容的用户输入当作纠错目标。

### 3. 简单去重

后端仍然会拦住几种明显坏输出：

- 空回复
- prompt 泄漏
- 把“translate this sentence”原样翻出来
- 机械重复上一条 assistant 回复
- 纠错时让用户重新发句子
- 纠错时直接把错误原句又吐回去

## 五、现在不再强依赖的旧能力

下面这些旧版重策略能力，已经不再作为核心分流逻辑：

- `greeting`
- `closing`
- `scene_setup`
- `correction_setup`
- `explanation`
- `language_switch`
- `roleplay`
- `mixed_practice`
- `general_tutor`

注意：

这些能力不是“完全不能做了”，而是“不再由后端硬编码成独立模式”。

现在更多是让模型自己根据统一 prompt 和历史对话来决定怎么回应。

## 六、当前生成参数

后端现在只按 3 类设置生成参数：

| focus | `max_new_tokens` | `temperature` | `top_p` | `repetition_penalty` |
| --- | ---: | ---: | ---: | ---: |
| `translation` | 180 | 0.25 | 0.80 | 1.08 |
| `correction` | 220 | 0.30 | 0.82 | 1.08 |
| `general` | 220 | 0.45 | 0.90 | 1.08 |

可以看到，整体上也比旧版更简单。

## 七、当前策略的优点

这版简化后，优点主要有：

- 不容易把微调模型带偏
- 不容易因为后端规则过多而误判用户意图
- 更适合评估微调模型本身能力
- 更容易观察模型真实短板，而不是被后端模板遮住

## 八、当前策略的风险

简化之后，也有几个要接受的代价：

- 模型自由度更高，偶发跑偏也可能更多
- 对“解释”“切语言”“收束任务”的控制会比旧版弱
- 某些复杂场景下，模型可能不如旧版那么稳定

所以这版更适合：

- 测试微调模型真实能力
- 观察模型原生表现
- 为后续继续微调收集真实坏样本

## 九、结论

当前后端已经从“强策略驱动”改成了“轻策略辅助”。

现在后端真正负责的事情只有三类：

1. 明确翻译请求
2. 明确纠错请求
3. 其他情况全部交给模型自然发挥

如果后面你继续微调模型，这版后端会更适合做真实效果观察，因为它不会再像以前那样，用一大堆规则把模型本身能力盖住。
