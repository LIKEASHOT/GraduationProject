# 普通对话模式下的对话生成代码结构

这份文档只针对**普通对话模式**，也就是 HTTP 文本接口 `/api/chat` 对应的回复生成链路。

目标是帮助后续评测基准模型（base）与微调模型（lora）在**普通文本对话模式**下的输出差异，而不是评测全双工语音模式。

---

## 1. 本次评测最核心的代码文件

### 1. [code/flask_app.py](<E:/Desktop/Graduation Project/code/flask_app.py:337>)

这是普通对话模式的**接口入口文件**。

普通模式下一次请求的大致流程在这里完成：

1. 读取前端传来的 `message`、`history`、`mode`、`model_variant` 等参数。
2. 合并并整理当前会话历史。
3. 调用 `DialoguePolicy.build_chat_prompt(...)` 构造最终 prompt。
4. 根据 `model_variant` 选择基准模型或 LoRA 微调模型。
5. 调用 `ConversationEngine.generate_response(...)` 生成回复。
6. 保存本轮对话历史。
7. 如果请求里需要音频，再继续走 TTS。

这段主链路的关键位置：

- [读取用户消息与历史](<E:/Desktop/Graduation Project/code/flask_app.py:337>)
- [读取模型变体 `model_variant`](<E:/Desktop/Graduation Project/code/flask_app.py:344>)
- [构造 prompt](<E:/Desktop/Graduation Project/code/flask_app.py:358>)
- [打印当前 intent 与 model_variant](<E:/Desktop/Graduation Project/code/flask_app.py:363>)
- [选择具体的 ConversationEngine](<E:/Desktop/Graduation Project/code/flask_app.py:368>)
- [调用 `generate_response`](<E:/Desktop/Graduation Project/code/flask_app.py:369>)

如果你要做普通模式评测，这个文件是**最先看的入口**。

---

### 2. [code/conversation_engine.py](<E:/Desktop/Graduation Project/code/conversation_engine.py:30>)

这是普通对话模式下真正的**LLM 推理核心文件**。

#### 主要职责

- 加载 Qwen 基座模型。
- 按需挂载 LoRA 适配器。
- 接收已经构造好的 prompt。
- 调用 `self.qwen_model.generate(...)` 生成文本。
- 对生成结果做基础清洗、重试和兜底。

#### 关键方法

- [ConversationEngine 类](<E:/Desktop/Graduation Project/code/conversation_engine.py:30>)
- [generate_response(...)](<E:/Desktop/Graduation Project/code/conversation_engine.py:160>)
  - 普通对话模式最核心的方法。
- [_generate_text_from_prompt(...)](<E:/Desktop/Graduation Project/code/conversation_engine.py:217>)
  - 真正调用模型生成。
- [self.qwen_model.generate(...)](<E:/Desktop/Graduation Project/code/conversation_engine.py:239>)
  - 底层 Hugging Face 生成调用。
- [_repair_response_with_policy(...) 开始处](<E:/Desktop/Graduation Project/code/conversation_engine.py:256>)
  - 如果回复被策略层判定不合格，会触发修复逻辑。
- [generate_response_stream(...)](<E:/Desktop/Graduation Project/code/conversation_engine.py:330>)
  - 这是流式文本生成入口，但普通 `/api/chat` 主要不是走它。

#### 普通模式里最重要的生成流程

`generate_response(...)` 内部大致顺序如下：

1. 如果 `use_context=True`，会再次调用 `DialoguePolicy.build_chat_prompt(...)`。
2. 否则会调用 `DialoguePolicy.ensure_system_prompt(...)`，确保传入内容已经是完整 prompt。
3. 调用 `DialoguePolicy.direct_response(...)` 检查是否存在直接返回逻辑。
4. 从 prompt 中提取最新用户文本与历史。
5. 调用 `DialoguePolicy.generation_config(...)` 获取当前生成参数。
6. 调用 `_generate_text_from_prompt(...)` 进行一次真实模型生成。
7. 调用 `_repair_response_with_policy(...)` 对不合格输出进行修复或重试。
8. 返回最终文本。

如果你想做**模型本体能力对比**，这个文件是最关键的。

---

### 3. [code/dialogue_policy.py](<E:/Desktop/Graduation Project/code/dialogue_policy.py:211>)

这是普通对话模式下的**策略层文件**，它不会替你加载模型，但会明显影响模型最终回复。

当前版本已经被简化为三类 focus：

- `translation`
- `correction`
- `general`

#### 主要职责

- 构造统一 prompt。
- 判断当前输入更偏向翻译、纠错还是普通对话。
- 根据 focus 给出不同生成参数。
- 判断模型输出是否需要重试。
- 在极端情况下返回兜底回复。

#### 关键方法

- [build_chat_prompt(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:211>)
  - 构造完整 prompt。
- [classify_user_intent(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:238>)
  - 当前只分 `translation / correction / general`。
- [generation_config(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:247>)
  - 为不同 focus 设置 `max_new_tokens / temperature / top_p / repetition_penalty`。
- [_build_unified_prompt(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:266>)
  - 当前普通模式最核心的 prompt 组织逻辑。
- [extract_state(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:425>)
  - 从历史里提取场景、语言偏好、上一次英文句子、用户事实等状态。
- [response_needs_retry(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:526>)
  - 判断模型回复是否需要重试。
- [fallback_response(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:561>)
  - 当模型回复一直不合格时的兜底回复。

#### 评测时为什么必须看这个文件

因为当前普通对话模式并不是“用户输入直接送进模型”。

真正送入模型之前，后端会：

1. 先分类用户意图。
2. 把历史整理成统一 prompt。
3. 根据任务类型改变生成参数。
4. 生成后再判断是否要重试。

所以如果你要比较 base 和 lora 的结果，必须知道：

- 是**模型本身**差异导致的输出变化。
- 还是**dialogue policy** 对两者都施加了相同约束之后形成的差异。

---

### 4. [code/speech_system.py](<E:/Desktop/Graduation Project/code/speech_system.py:230>)

这个文件在普通对话评测里不是第一核心，但有一层包装作用。

这里的：

- [generate_response(...)](<E:/Desktop/Graduation Project/code/speech_system.py:230>)

本质上只是继续调用：

- `self.conversation_engine.generate_response(...)`

所以它更像系统整合层，不是最底层生成逻辑。

如果只是做普通对话模型评测，可以把它看成一个薄封装。

---

## 2. 与模型切换相关的代码位置

### [code/flask_app.py](<E:/Desktop/Graduation Project/code/flask_app.py:47>)

普通模式里，base 和 lora 的切换主要发生在这个文件。

关键位置如下：

- [active_model_variant = \"base\"](<E:/Desktop/Graduation Project/code/flask_app.py:47>)
  - 当前后端启动后的默认模型变体。

- [_get_model_variant(data)](<E:/Desktop/Graduation Project/code/flask_app.py:106>)
  - 从请求体里读取这次到底用 `base` 还是 `lora`。

- [_get_conversation_engine_for_variant(model_variant)](<E:/Desktop/Graduation Project/code/flask_app.py:124>)
  - 根据变体返回对应的 `ConversationEngine`。

- [启动时读取环境变量并决定默认模型](<E:/Desktop/Graduation Project/code/flask_app.py:171>)
  - 会设置 `active_model_variant`。

因此：

- 你要测**基准模型**，普通模式请求应走 `model_variant=base`。
- 你要测**微调模型**，普通模式请求应走 `model_variant=lora`。

---

## 3. 普通对话模式的真实调用链

普通模式下一次文本生成，可以概括为：

1. 前端调用 `/api/chat`
2. [flask_app.py](<E:/Desktop/Graduation Project/code/flask_app.py:337>) 读取消息与历史
3. [DialoguePolicy.build_chat_prompt(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:211>) 构造 prompt
4. [flask_app.py](<E:/Desktop/Graduation Project/code/flask_app.py:368>) 选择 base 或 lora 的 `ConversationEngine`
5. [ConversationEngine.generate_response(...)](<E:/Desktop/Graduation Project/code/conversation_engine.py:160>) 进入生成主流程
6. [DialoguePolicy.generation_config(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:247>) 提供生成参数
7. [self.qwen_model.generate(...)](<E:/Desktop/Graduation Project/code/conversation_engine.py:239>) 生成文本
8. [DialoguePolicy.response_needs_retry(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:526>) 判断是否重试
9. 返回最终回复文本

可以把它理解成：

`flask_app.py -> dialogue_policy.py -> conversation_engine.py -> qwen_model.generate() -> dialogue_policy.py`

---

## 4. 对普通模式评测最有价值的文件分层

### A. 必看文件

- [code/flask_app.py](<E:/Desktop/Graduation Project/code/flask_app.py>)
- [code/conversation_engine.py](<E:/Desktop/Graduation Project/code/conversation_engine.py>)
- [code/dialogue_policy.py](<E:/Desktop/Graduation Project/code/dialogue_policy.py>)

这三个文件已经足够支撑你理解普通模式的对话生成与模型切换。

### B. 可选参考文件

- [code/speech_system.py](<E:/Desktop/Graduation Project/code/speech_system.py>)

如果只做普通文本对话评测，这个文件不是重点。

### C. 本次可先忽略的文件

以下文件不属于“普通文本对话模式”的核心生成链路，本次评测可以先不喂给评测 Codex：

- `full_duplex_backends.py`
- `full_duplex_session.py`
- `asgi_app.py`
- `tts_engine.py`
- `audio_processor.py`
- `full_duplex_vad.py`

原因是：

- 它们主要对应全双工语音模式、ASR、TTS 或实时会话调度。
- 你当前只想评测普通对话模式下，base 和 lora 的文本回复差异。

---

## 5. 评测时建议重点观察的变量

如果后续要让另一个 Codex 分别生成“微调前 / 微调后”的回复，建议它重点关注这些影响因素：

### 1. 模型变体

- `model_variant=base`
- `model_variant=lora`

### 2. prompt 是否一致

要确保两边都走同样的：

- `history`
- `user_message`
- `DialoguePolicy.build_chat_prompt(...)`

否则评测结果会混入 prompt 差异。

### 3. 生成参数是否一致

当前生成参数并不是写死在模型调用里，而是来自：

- [DialoguePolicy.generation_config(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:247>)

而且还可能被环境变量覆盖：

- `QWEN_MAX_NEW_TOKENS`
- `QWEN_TEMPERATURE`
- `QWEN_TOP_P`
- `QWEN_REPETITION_PENALTY`

### 4. 是否经过 retry / fallback

如果某个回复被：

- [response_needs_retry(...)](<E:/Desktop/Graduation Project/code/dialogue_policy.py:526>)

判定为不合格，那么最终返回的内容可能不是第一轮原始生成，而是：

- 重试后的结果
- 或 fallback 结果

因此在评测时，最好区分：

- **原始模型首轮输出**
- **经过策略修复后的最终输出**

---

## 6. 这份代码结构文档的用途

这份文档适合喂给另一个专门做评测或微调分析的 Codex，让它基于当前普通模式代码链路做以下工作：

1. 只关注 `/api/chat` 普通对话模式。
2. 明确 base 和 lora 的切换位置。
3. 理解 prompt 是怎么构造的。
4. 理解模型输出为什么会受 `dialogue_policy.py` 影响。
5. 在评测时区分“模型差异”和“策略差异”。

如果目标是生成一组“微调前后回复对比样本”，那么最应该围绕的文件就是：

- [code/flask_app.py](<E:/Desktop/Graduation Project/code/flask_app.py>)
- [code/conversation_engine.py](<E:/Desktop/Graduation Project/code/conversation_engine.py>)
- [code/dialogue_policy.py](<E:/Desktop/Graduation Project/code/dialogue_policy.py>)

这三者共同决定了普通对话模式下最终看到的回复。
