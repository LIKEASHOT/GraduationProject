# 第3章 中英双语语音对话大模型关键技术研究方法

## 3.1 整体实验方案设计

### 3.1.1 研究目标与实验思路

结合《项目介绍与理解文档》及《模型微调过程说明文档》的内容，本文的研究目标并非搭建一个面向开放域闲聊的通用聊天系统，而是面向英语学习与口语陪练场景，构建具有中英双语语音交互能力的对话系统，并围绕其中的大语言模型场景适配问题展开实验研究。近年来，语音对话系统在技术路线上通常可以分为级联式与端到端两类，其中 ASR—LLM—TTS 级联方案仍然具有模块边界清晰、行为可控性较强、便于面向场景进行针对性优化等特点；同时，Qwen3 系列模型具备较强的多语言指令跟随、角色扮演与多轮对话能力，为本课题提供了较合适的基座模型条件[5][11][22]。

本文的总体思路是将系统方法设计与模型微调方法设计结合起来：一方面，以语音识别、语义理解、对话生成与语音合成为基本链路，构建中英双语语音对话系统；另一方面，围绕英语教学、口语陪练、翻译解释、轻量纠错、情景推进与自然收束等具体需求，对基座模型进行面向任务的数据构造和参数高效微调。由此，第三章关注的核心问题不是“系统能否运行”，而是“系统为何采用这样的技术路线、模型为何需要这样的数据和微调方式、实验如何围绕场景目标进行组织”。

### 3.1.2 系统总体技术路线

本文采用 ASR—LLM—TTS 的级联式技术路线组织整体实验。系统首先接收用户的中文、英文或中英混合语音输入，并通过自动语音识别模块完成转写；随后，将识别结果连同对话历史、场景状态及系统角色约束一并输入大语言模型，由模型完成语义理解、对话生成、翻译解释、纠错反馈与情景推进；最后，将生成的中英双语文本回复送入语音合成模块，输出适合口语陪练场景的语音反馈。这一路线与当前对级联式 spoken dialogue model 的典型描述基本一致，但在本课题中进一步强调了双语输入输出、教学型反馈和多轮交互连贯性等场景要求[5]。

与一般文本对话系统相比，本文的方法设计更强调语音链路中的误差传递与场景约束。ASR 输出的文本不仅承担“转写”功能，还直接影响后续语义理解和教学反馈质量；LLM 不仅需要生成语言自然的回复，还需要在角色扮演、纠错粒度、问题推进方式和中英解释比例之间取得平衡；TTS 也并非简单读出生成文本，而是服务于口语练习中的可听性、简洁性和交互节奏。因此，本章的方法组织以“语音链路构建—模型微调—数据迭代—评测设计”为主线展开。

### 3.1.3 实验内容与章节安排

基于上述目标与思路，本文首先对中英双语语音对话系统的整体架构、级联链路及多轮上下文管理方法进行说明；随后，围绕 Qwen3-4B 基座模型介绍场景化微调方法，重点整理 SFT 数据集的来源、构造方式和 LoRA 微调实验设计；在此基础上，进一步归纳数据版本迭代与专项能力增强的方法特征；最后，对本文的评测任务设计、人工测试关注维度以及基座模型与微调模型的对比思路进行说明。上述内容共同构成后续实验结果与分析章节的前提。

## 3.2 中英双语语音对话系统方法设计

### 3.2.1 系统整体架构

系统整体架构如图3-1所示。本文构建的中英双语语音对话系统以“语音输入—文本理解与生成—语音输出”为主干流程，并在中间引入对话上下文管理与场景状态控制机制。对于英语学习和口语陪练场景而言，系统不仅要完成信息传递，更要承担教师型引导、角色设定维持、学习目标推进和反馈解释等任务。因此，系统架构并非简单地将 ASR、LLM 和 TTS 顺序串接，而是在 LLM 环节中叠加了与场景相关的提示词约束、对话历史组织和专项能力调用逻辑，使其能够根据当前轮次完成翻译、纠错、解释、追问或收束等不同功能。

从方法层面看，该架构的核心思想在于以 LLM 作为中间语义枢纽，将前端输入的不确定语音文本和后端输出的可播报语音文本连接起来。这样设计的好处在于，可以将双语识别误差修正、上下文语义补全、教师式反馈生成等任务集中在语言模型层完成，从而使系统在面对中英混合输入、口语表达不完整或学习者表达偏误时，仍具备较强的交互弹性。

### 3.2.2 ASR、LLM 与 TTS 级联链路设计

在级联链路中，ASR 模块负责接收用户语音输入并输出可供后续处理的文本结果。与普通单语识别不同，本课题中的 ASR 面向的是中英双语甚至中英混合的真实口语输入，这意味着系统需要处理语码切换、非母语口音、停顿重复和局部表达不完整等现象。相关研究指出，code-switching ASR 在数据稀缺和语言切换边界处理方面仍具有较高难度[2]。因此，本文在方法设计上并不将 ASR 输出视为绝对可靠文本，而是将其作为“待语义确认”的中间结果，并在 LLM 侧通过 asr_recovery 等任务能力进行适度恢复和纠偏。

LLM 模块是整条链路中的核心决策单元。本文并未将其限定为单一的文本续写器，而是赋予其语义理解、双语转换、教学反馈生成、情景推进和对话收束等复合职责。具体而言，当输入来自英语口语陪练场景时，模型需要判断用户的表达意图、当前练习阶段、是否存在明显语言错误、是否需要给出中文解释，以及是否应继续推动情景展开。相较于将翻译、纠错、问答分别交由不同模型处理的方案，这种以 LLM 为中心的组织方式更有利于保持回复风格一致性、反馈逻辑连贯性和场景角色稳定性。

TTS 模块则负责将文本回复转换为语音输出。由于本系统服务于口语训练场景，因此其输出不仅要求可懂，还需要尽量保证回复长度适中、语句边界清晰、双语混读时不产生明显断裂，并与前文的交互节奏保持一致。由此可见，ASR、LLM 与 TTS 在方法上并非独立存在，而是通过中间文本表示与上下文状态持续发生耦合，这也是本文后续引入数据迭代和专项能力增强的重要原因。

### 3.2.3 多轮语音交互与上下文管理方法

多轮交互是英语口语陪练系统区别于单轮问答系统的重要特征。本文采用基于对话历史的上下文管理方式，将历史轮次中的 system、user、assistant 信息组织为连续消息序列，并在每轮推理时与当前输入共同送入语言模型。这样做的目的在于保留场景设定、用户偏好、前文纠错点、未完成任务以及最近一轮互动结果，从而提升回复的承接性与一致性。

在具体方法上，上下文管理不仅服务于“记忆”，还服务于“控制”。对于教师型陪练系统而言，模型如果只依赖当前输入，容易出现重新开场、忽略前文要求、重复提问或在情景接近结束时继续机械追问等问题。为减少这类现象，本文在消息组织中强调场景阶段信息与交互目标的持续传递，使模型能够判断当前应当继续推进、进行纠错说明，还是进行自然收束。此外，由于语音交互比文本交互更依赖节奏和连贯性，本文在多轮方法上进一步强调回复应短促、明确、便于 TTS 播报，并尽量保持一轮一意图、一轮一推进点的交互方式。相关综述也指出，多轮 spoken dialogue 的关键问题正包括上下文保持、交互连续性与低时延自然响应[5]。

## 3.3 大语言模型场景化微调方法

### 3.3.1 基座模型选择与微调目标

本文选取 Qwen3-4B 作为基座模型。其一，Qwen3 系列本身面向多语言理解与生成能力进行了扩展，公开资料显示其支持百余种语言和方言，并在角色扮演、多轮对话和指令跟随方面具有较好的基础能力[11][22]。citeturn3view0turn3view1 其二，4B 级别参数规模在本课题的实验条件下较易开展多轮迭代微调与验证，有利于在模型能力与实验可行性之间取得平衡。其三，作为面向对话任务的开源基座，Qwen3-4B 便于围绕英语教学场景进行后续行为塑形和专项能力增强。

本文的微调目标并不是提升模型的通用知识覆盖范围，而是增强其在英语学习和口语陪练场景中的任务适配能力。具体而言，模型需要在真实交互中表现出较稳定的教师型行为，包括以自然口吻推进角色扮演、根据用户偏好调整对话方向、在必要时提供轻量纠错和中英双语解释、在识别有误或句意不完整时进行合理恢复，并在情景完成后实现自然收束。监督微调实质上是通过任务化样本进一步缩小“通用语言模型目标”与“场景化指令执行目标”之间的差距，这也是 instruction tuning 或 SFT 在实践中的核心意义[23]。

### 3.3.2 SFT 数据集来源与构造方法

本文使用的 SFT 数据并非直接套用通用开源对话语料，而是围绕项目目标进行面向任务的构造。根据已有材料可整理出，数据来源主要包括人工整理的教学型对话样本、真实测试交互的改写样本、失败案例的修正样本，以及在统一风格约束下通过 API 扩展生成的补充样本。这样做的目的，在于使训练数据与系统目标保持一致，即数据本身就体现英语陪练系统希望获得的行为特征，而不是依赖模型在通用语料中自行涌现。

在数据处理流程上，原始样本在进入训练集之前还经历了规则过滤、去重、切分与格式统一等整理过程。数据组织采用多轮 messages 结构，基本单元由 system、user 和 assistant 三类消息构成，以适配对话模型的训练格式。其中，system prompt 并非完全固定，而是会随着任务类别、场景状态和教学目标发生变化。例如，在口语陪练场景中，system 更强调回复简洁、自然追问和角色维持；在翻译解释场景中，则更强调双语转换与简明说明；在对话收束场景中，则会强化结束语气、总结与温和收尾。这样的构造方式使 system 不再只是通用身份设定，而成为控制模型行为边界和交互风格的重要变量。

从能力组织角度看，数据大致围绕 teacher_push、scene_continuity、preference_following、correction、asr_recovery、mixed_explanation、conversation_closing、translation 等类别展开。teacher_push 用于塑造教师型陪练者的主动引导能力，scene_continuity 用于保持角色扮演或练习情境的连续性，preference_following 用于提高模型对用户选择和练习意图的遵循程度，correction 用于实现不过度打断的轻量纠错，asr_recovery 用于增强对识别噪声和口语残缺的容错能力，mixed_explanation 用于生成适合中国学习者理解的中英混合解释，conversation_closing 用于训练自然收束能力，translation 则用于维持中英双向翻译与解释能力。可以看出，数据构造并非对任务标签的简单分类，而是围绕“系统需要学会什么行为”来设计样本结构，这与 SFT 通过监督样本强化模型可控性的基本思想是一致的[23]。

### 3.3.3 LoRA 微调实验设计

在微调实现上，本文采用监督微调与 LoRA 相结合的方式开展实验。LoRA 的基本思想是在冻结预训练模型主体参数的前提下，将可训练的低秩增量注入 Transformer 的相关层中，以较小的参数更新量实现面向下游任务的适配[20][13]。 结合本课题的实验目标，本文将 LoRA 作为主要的参数高效微调方案，重点作用于注意力相关投影层，以在尽量保留基座模型通用能力的同时，强化其在英语教学场景中的特定行为表现。

LoRA 的参数更新形式可表示为：

\[
W = W_0 + \Delta W,\qquad \Delta W = \frac{\alpha}{r}BA
\tag{3-1}
\]

其中，\(W_0\) 表示原始权重矩阵，\(\Delta W\) 表示低秩增量，\(B\) 与 \(A\) 为可训练矩阵，\(r\) 为低秩分解的秩，\(\alpha\) 为缩放系数。通过这种方式，训练过程不必对全部参数进行更新，而是将学习重点集中在少量新增参数上。对于本文这种需要多轮实验迭代、持续补充专项数据并反复比较模型行为变化的任务而言，LoRA 能够降低实验成本，同时提升实验组织的灵活性。

在实验组织上，本文采用训练集与验证集划分的方式实施监督微调，并通过按阶段、按数据版本命名的输出目录管理不同轮次实验结果，使各版本模型具有较好的可回溯性和可比较性。训练脚本中的模型路径、数据文件、输出目录、日志记录、评估与保存间隔等设置，在本文中不被视为工程命令细节，而是构成实验方法的一部分。因为对于多轮式场景微调而言，清晰的版本管理、稳定的训练流程和一致的评测条件，是后续比较“数据补充是否有效”“专项能力是否增强”的必要前提。

## 3.4 数据版本迭代与专项能力增强方法

### 3.4.1 基础行为塑形阶段

数据版本迭代与专项增强流程如图3-2所示。本文的第一阶段主要完成基础行为塑形，即通过首轮任务化数据使模型形成相对稳定的 oral coach 行为模式。从已有材料看，这一阶段的重点并不在于扩大知识覆盖，而在于塑造交互风格和行为边界，包括回复尽量简洁、自然开展角色扮演、一次只推进一个有效问题、保持场景连续性、遵循用户偏好、进行温和纠错、对 ASR 噪声具备一定容忍度，并在必要时给出简洁的中英双语解释。基础行为塑形的意义在于先让模型“像一个合适的陪练对象”，再进一步追求专项能力的精修。

### 3.4.2 面向对话收束能力的 closing 数据补充方法

在基础版本形成之后，人工测试暴露出模型在对话末尾存在一定过度追问倾向，即使用户已经表达结束意图或场景已经基本完成，模型仍可能继续机械推进下一轮问答。针对这一问题，本文并未仅通过修改提示词进行表面修补，而是采用补充 closing 数据并重新训练的方式进行专项增强。相关数据重点覆盖情景结束、任务完成、用户明确表示停止练习、完成总结后自然收尾等情形，使模型学习在不同结束条件下给出简洁、礼貌且不拖沓的结束性回复。

这种修正方式体现出本文实验设计中的一个重要特征，即当模型问题不是“知识不会”，而是“行为不稳”时，数据补充应围绕失败现象本身展开。closing 数据的加入，不是为了增加一类孤立能力，而是为了改善多轮语音交互中的整体节奏，使模型既能推进学习，又能够识别交互终点，从而提高系统的自然性与可用性。

### 3.4.3 面向翻译能力恢复的 translation 数据补充方法

在后续迭代中，项目材料又显示，随着模型逐步向教学型陪练风格靠拢，其部分翻译能力出现相对削弱现象，表现为中英转换不够稳定、解释型翻译不足或双语回答不够准确。针对这一问题，本文进一步补充 translation 数据，并将其作为单独的专项增强方向纳入训练。该类数据既包括较直接的中译英、英译中任务，也包括结合口语场景的短句释义、表达替换、双语解释与教学型翻译反馈。

需要指出的是，translation 数据的加入并不是简单叠加“翻译样本数量”，而是在已有教师型对话框架中恢复和强化模型的双语转换能力，使其与 correction、mixed_explanation、scene_continuity 等能力保持一致。换言之，本文所追求的不是一个孤立的翻译器，而是一个能够在英语学习场景中自然完成翻译、解释和推进对话的双语语音对话模型。

## 3.5 实验评测任务与对比思路

### 3.5.1 评测目标与任务划分

本文的评测设计以“英语教学场景适配能力验证”为核心，而不是以通用问答正确率作为唯一依据。已有研究表明，面向英语口语练习的 AI 聊天机器人研究仍处于持续拓展阶段，教学场景中的评价重点通常不仅包括回答内容本身，还涉及学习支持效果、互动方式与场景适配性[9]。 基于此，本文将评测任务划分为若干与项目目标高度相关的类别，包括翻译能力、轻量纠错能力、角色扮演推进能力、上下文保持能力、收束能力、重复率控制以及中英语言切换稳定性等。对于语音链路而言，还需结合 ASR 输入的不确定性，观察模型在识别误差条件下的恢复与容错表现。

这些测试任务既覆盖单轮能力，也覆盖多轮流程能力。前者用于观察模型是否具备特定任务的基础响应质量，后者则用于检验模型在连续语音互动中能否维持场景、角色和教学策略的一致性。这样的划分方式，与本文“系统方法设计+场景微调实验”并行推进的研究路径相匹配。

### 3.5.2 人工测试关注维度

考虑到本课题面向真实口语陪练场景，且部分目标难以通过单一自动指标充分刻画，本文以人工测试作为主要评测方式之一。人工测试重点关注以下几个方面：其一，回复是否能够准确承接用户当前表达和前文场景；其二，是否体现出教师型引导而非泛化闲聊；其三，纠错是否适度、清晰且不过度打断交流；其四，中英双语解释是否简洁、自然、便于学习者理解；其五，回复长度是否适合 TTS 播报；其六，在多轮交互中是否存在重复追问、突然跳题或难以结束的问题。

此外，人工测试还特别关注语言切换稳定性与 ASR 容错能力。由于用户输入可能包含中文提示、英文练习句和中英夹杂表达，模型若不能正确理解当前语言使用意图，便容易在后续回复中出现解释过多、练习不足或语言切换失衡等现象。因此，人工测评不仅面向“答得对不对”，还面向“是否适合这个学习场景”。

### 3.5.3 基座模型与微调模型的对比思路

为验证微调策略的有效性，本文采用基座模型与微调模型对比的方式组织实验。对比时尽量保持输入场景、system 约束、用户表达和上下文轮次条件一致，以观察模型在相同任务下的行为差异。比较重点不在于通用知识问答能力，而在于是否更符合英语学习和口语陪练的场景要求，包括是否更会推进角色扮演、是否更能遵循用户偏好、是否更擅长给出轻量纠错和中英解释、是否具备更自然的收束能力，以及是否在多轮语音交互中表现出更好的稳定性。

除基座模型与最终微调模型的对比外，本文还重视不同数据版本对应模型之间的阶段性比较。通过这种组织方式，可以更清楚地考察基础行为塑形、closing 数据补充和 translation 数据补充分别对系统能力产生怎样的影响。结果将在后续章节中结合具体测试记录展开分析，而本章的任务是说明这种对比实验如何被设计和实施。

## 3.6 本章小结

本章围绕“本文如何开展中英双语语音对话大模型研究”这一核心问题，对实验方法进行了系统说明。首先，本文明确了面向英语学习与口语陪练场景的研究目标，并提出 ASR—LLM—TTS 的级联式技术路线；其次，从系统层面对整体架构、链路设计和多轮上下文管理方法进行了整理；随后，结合 Qwen3-4B 基座模型，说明了基于 SFT 与 LoRA 的场景化微调方法，以及服务于教学型能力塑造的数据构造逻辑；在此基础上，又归纳了以问题发现为驱动的数据版本迭代与专项增强方法；最后，对评测任务与对比思路进行了设计说明。上述内容为后续实验结果分析与系统效果讨论提供了方法基础。

## 参考文献

[30] JI S, CHEN Y, FANG M, 等. WavChat: A survey of spoken dialogue models[EB/OL]. (2024-11-15)[2026-04-22]. https://arxiv.org/abs/2411.13577.  
[31] Qwen Team. Qwen3 technical report[EB/OL]. (2025-05-14)[2026-04-22]. https://arxiv.org/abs/2505.09388.  
[32] Qwen Team. Qwen3-4B[EB/OL]. (2025-05-14)[2026-04-22]. https://huggingface.co/Qwen/Qwen3-4B.  
[33] XI Y, DING W, YU K, 等. Semi-supervised learning for code-switching ASR with large language model filter[EB/OL]. (2024-07-05)[2026-04-22]. https://arxiv.org/abs/2407.04219.  
[34] ZHANG S, DONG L, LI X, 等. Instruction tuning for large language models: A survey[EB/OL]. (2023-08-21)[2026-04-22]. https://arxiv.org/abs/2308.10792.  
[35] HU E J, SHEN Y, WALLIS P, et al. LoRA: Low-rank adaptation of large language models[EB/OL]. (2021-06-17)[2026-04-22]. https://arxiv.org/abs/2106.09685.  
[36] WANG L, CHEN S, JIANG L, et al. Parameter-efficient fine-tuning in large models: A survey of methodologies[EB/OL]. (2024-10-24)[2026-04-22]. https://arxiv.org/abs/2410.19878.  
[37] DU J, DANIEL B K. Transforming language education: A systematic review of AI-powered chatbots for English as a foreign language speaking practice[J]. Computers and Education: Artificial Intelligence, 2024, 6: 100230.

## 公式代码

公式（3-1）的 LaTeX 代码如下：

```latex
\[
W = W_0 + \Delta W,\qquad \Delta W = \frac{\alpha}{r}BA
\tag{3-1}
\]
```

## 建议配图及生成提示词

图名：图3-1 中英双语语音对话系统总体架构图  
提示词：请绘制一张适用于中文计算机类本科毕业论文的系统架构示意图，风格简洁、学术化、白底、扁平化、适合 Word 排版。图中采用从左到右的流程布局，包含以下模块与箭头关系：用户语音输入 → ASR 中英双语/中英混合语音识别 → 识别文本与噪声恢复线索 → 对话上下文管理模块 → Qwen3-4B 大语言模型（标注“SFT+LoRA 场景化适配”）→ 回复文本生成 → TTS 中英双语语音合成 → 语音输出。其间在大语言模型模块旁添加功能标签：翻译、纠错、双语解释、角色扮演推进、对话收束。上下文管理模块需要与大语言模型双向连接，并标注“历史轮次、场景状态、用户偏好、学习目标”。整体色彩以蓝灰色为主，结构清晰，不要添加商业化装饰元素。

图名：图3-2 数据版本迭代与专项能力增强流程图  
提示词：请绘制一张适用于中文计算机类本科毕业论文的流程图，白底、简洁、扁平化、学术风格。流程从上到下展示模型微调数据的版本迭代过程：任务化数据初始构造（人工整理、真实对话改写、失败案例修正、API 扩展生成、规则过滤、去重切分）→ 基础行为塑形微调 → 人工测试与问题发现 → 补充 closing 数据 → 重新训练 → 再次人工测试与问题发现 → 补充 translation 数据 → 再训练形成最终版本。请在图右侧用小标签列出能力类别：teacher_push、scene_continuity、preference_following、correction、asr_recovery、mixed_explanation、conversation_closing、translation。要求图中清楚体现“问题发现—专项补数—重新训练”的迭代逻辑，颜色以蓝色和浅橙色区分“训练阶段”和“问题修正阶段”。
